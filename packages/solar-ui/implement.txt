ravel-ui — Implementation Log
==================================

Phase 1 — Token expansion + atom upgrades  (2026-04-26, in progress)
--------------------------------------------------------------------

Goal: bring ravel-ui to parity with the Hi-Fi mock's terminal screen so
frontend/ can compose pages without redefining visual primitives.

Tokens
------
- theme.css: kept Tailwind v4 @theme block; added a semantic CSS-var layer
  (:root + html[data-theme=dark|light] + html[data-accent=amber|ion|signal|
  violet]) so the same markup re-skins at runtime via data attributes.
  Variables exposed: --bg/-2, --surface/-hi/-lo, --line/-hi, --fg/-2/-3/-4,
  --green, --red, --accent/-soft/-dim, --on-accent, --glow.
- fonts.css: added Space Mono (used in Hi-Fi for tags, prices, mono labels).
- base.css: added .card-glow (mask-composite gradient hairline rim from Hi-Fi
  .card::after), .hud-corner (4 positional variants), live-dot-pulse +
  mic-ping keyframes; rewrote .solar-orb to match Hi-Fi's accent-driven
  radial gradient + dual glow shadow.

Atoms upgraded
--------------
- Text: added 'tag' (Space Mono eyebrow) + 'mono' (tabular numerics)
  variants; introduced `tone` prop (default/muted/subtle/accent/up/dn) wired
  to semantic vars instead of hardcoded white-alpha.
- Card: added `glow` prop (renders .card-glow), exposed Card.Header sub-
  component matching Hi-Fi `.hd > h3 + tag` pattern. All bg/border now
  reference semantic vars.
- Chip: added `bordered` variant (rebalance-rail style — outline that flips
  to accent on hover).
- Button: added `deploy` variant (gradient accent->soft + glow shadow,
  used in voice footer + rebalance CTA).
- Divider: switched root element to <hr> for a11y compliance; added
  `dashed` prop for watchlist row separators.
- ProgressBar: added `bidirectional` mode — fill grows from center,
  positive=accent right, negative=muted left. Powers rebalance drift bars.

Atoms added
-----------
- Kbd: inline keyboard hint (⌘K) — rendered as <kbd>.
- LiveDot: pulsing status indicator. Optional `label` renders the dot +
  uppercase mono label inline (covers "LIVE · NSE" and similar).
- HudCorners: wraps content in 4 accent corner brackets via .hud-corner.
  Each corner individually toggleable.
- Sparkline: SVG line/area chart. Auto-normalizes points to viewBox; tone
  switches between up/dn/accent/neutral; optional fill draws a gradient
  area below the line.
- CountUp: client-only animated number with cubic-out easing. Supports
  Indian-grouping (12,84,500) when format=inr, else locale formatting.
  Re-animates on value change.

Barrel
------
- src/index.ts: exports Kbd, LiveDot, HudCorners, Sparkline, CountUp +
  their prop types, alongside existing primitives.

Verification
------------
- `pnpm build` → ESM + CJS + DTS clean. dist/index.js: 28.6 KB ESM.
- `pnpm type-check` → no errors.
- IDE Biome warning on index.ts (sort imports) is Information-level only.
- Pre-existing frontend type errors in ScreenerPicks.tsx are unrelated
  (data type from useScreenerPicks not annotated upstream) and not
  introduced by Phase 1.

Phase 2 — Molecules + layout shells  (2026-04-26, complete)
-----------------------------------------------------------

Molecules
- Stat: label · value · delta · optional Sparkline; Hi-Fi top-left
  accent corner ticks rendered via two absolute spans.
- WatchRow: icon-square + symbol/sub + price + change. Click handler
  promotes root to <button>; non-interactive renders as <div>.
- RiskBars: N vertical fills with one accent-glowing active bar; values
  in 0..100, height in px.
- Ticker (client): horizontal marquee, doubles the item list inline so
  CSS animation can translate -50% for seamless loop. No JS interval.
- MicIndicator: circular accent chip + 2 staggered ping rings (uses
  base.css `mic-ping` keyframes).
- Waveform: 8-bar audio meter with per-bar staggered animation-delay;
  decorative only.

Layout shells
- AppShell: header / ticker / rail / main / footer slots; provides the
  radial-gradient ambient background and uses semantic --bg / --glow vars
  so it re-skins on data-theme/data-accent flip.
- TopBar: brand + nav (active gets underline glow) + right slot. Bottom
  hairline rendered via ::after gradient.
- IconRail: 52px-wide vertical rail of icon buttons; active item gets
  accent border, tinted bg, inner glow.
- VoiceDock: footer with left accent stripe (::before gradient); slots
  for mic, center (waveform + transcript), cta.

Phase 3 — Portfolio + boot atoms  (2026-04-26, complete)
--------------------------------------------------------
- SegmentedControl<T> (client): generic radiogroup; active segment
  fills with accent. Used by theme switcher + portfolio Treemap/Ledger
  toggle.
- Swatches (client): 4 colored circles (amber/ion/signal/violet) with
  outline-focused active state. Exports `AccentName` type.
- BootStep: one row of the boot list (queued/now/ok states drive icon
  glyph + colour; `now` spins via local @keyframes boot-spin).
- TreemapCell: absolute-positioned cell; bg auto-tints by pnlPct via
  color-mix(--green/--red, intensity). Optional decoration slot for
  inline sparklines.

Phase 4 — Theme runtime  (2026-04-26, complete)
-----------------------------------------------
- ThemeProvider + useTheme(): sets data-theme + data-accent on
  <html>, persists to localStorage under "solar-orb-theme". Hydrates
  from storage in a post-mount effect (SSR-safe). Optional `ephemeral`
  prop disables persistence for screenshot/SSR-only renders.
- useTheme throws if used outside provider — fail loud rather than
  silently fall back, since wiring it up is a one-liner.

Barrel + verification
---------------------
- src/index.ts re-exports all 28 components + types + tokens.
- `pnpm build` clean: ESM 49.7 KB / CJS 52.8 KB / DTS 26.6 KB.
- `pnpm type-check` clean.
- IDE Biome `useSortedKeys` info on barrel grouping is intentional
  (atoms/molecules/shells grouped by category, alpha within each).

Companion package
-----------------
- Spun out the central Solar Orb into @alphaforge/solar-orb-ball (own
  Vite dev server + playground). See packages/solar-orb-ball/implement.txt.

Next
----
- Refactor frontend/src/app/page.tsx + terminal/* onto the new
  primitives (delete `floating-shard` ad-hoc styling).
- Build /portfolio and /boot routes against TreemapCell + BootStep.
- Wire ThemeProvider into root layout + add a settings popover that
  combines SegmentedControl (theme) + Swatches (accent).
