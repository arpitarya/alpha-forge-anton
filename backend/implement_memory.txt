Vector DB / Memory Lake — Implementation Log
==============================================
Date: 2026-04-22
Status: Complete

## What Was Built

### Infrastructure
- pgvector v0.8.0 compiled from source for PostgreSQL 16 (Homebrew brew formula only ships PG17+)
- pgvector extension enabled on alphaforge_anton database
- `pgvector>=0.3.0` Python package added to backend/pyproject.toml

### New Files
- backend/app/models/memory.py     — ScreenerPickEmbedding + ConversationMemory ORM models
- backend/app/services/embedding.py — EmbeddingService (Gemini text-embedding-004, 768-dim, free tier)
- backend/app/services/memory.py   — MemoryService: index_pick, search_picks, save_turn, search_memory
- backend/alembic/versions/640eee61bc50_initial_schema_with_pgvector_memory.py — full schema migration

### Modified Files
- backend/app/core/config.py        — embedding_model, embedding_dimensions, memory_top_k, etc.
- backend/app/models/__init__.py    — exports ScreenerPickEmbedding, ConversationMemory
- backend/alembic/env.py            — import new models for autogenerate tracking
- backend/app/services/ai_service.py — full RAG chat() implementation using MemoryService
- backend/app/routes/ai.py          — ChatResponse.session_id/sources, /ai/memory/search endpoint
- backend/app/routes/screener.py    — background embed on push, /screener/picks/embed-backfill
- backend/app/main.py               — embedding service warmup/close in lifespan()
- llm-gateway/src/alphaforge_anton_llm_gateway/types.py   — RAG_CHAT QueryType
- llm-gateway/src/alphaforge_anton_llm_gateway/router.py  — RAG_CHAT routing (Gemini → Groq → Ollama)
- llm-gateway/src/alphaforge_anton_llm_gateway/prompts.py — RAG_CHAT system prompt

## Key Decisions
- Used FastAPI BackgroundTasks for pick embedding (not Celery) to avoid extra broker dependency
- 1.1s sleep between Gemini embedding calls (free tier: 1 RPM)
- Mock embedding ([0.0]*768) when GEMINI_API_KEY absent (dev/CI mode)
- Idempotent pick indexing: (symbol, scan_date, model_type) unique constraint

## Endpoints Added
- GET  /api/v1/ai/memory/search?q=...   — semantic search over picks + conversations
- POST /api/v1/screener/picks/embed-backfill — re-embed all existing JSON pick files

## How to Test
1. Verify DB: psql -d alphaforge_anton -c "\dt"
2. Add GEMINI_API_KEY to backend/.env
3. POST picks to /api/v1/screener/picks  → check logs for "Background embed"
4. GET /api/v1/ai/memory/search?q=stocks+with+high+ADX → returns ranked picks
5. POST /api/v1/ai/chat with session_id in context → reply includes sources[]
