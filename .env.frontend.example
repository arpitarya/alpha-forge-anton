# ══════════════════════════════════════════════════════════════
# AlphaForge Anton Frontend — Environment Configuration
# ══════════════════════════════════════════════════════════════
# Copy to .env.local (Next.js auto-loads this file).
# Service ports live in .env.port at the repo root.
# Only NEXT_PUBLIC_ vars are exposed to the browser bundle.
# ══════════════════════════════════════════════════════════════

# ── Logging (server-only) ───────────────────────────────────
LOG_LEVEL=info                            # trace | debug | info | warn | error | fatal
LOG_DIR=logs                              # Directory for log files (relative to frontend/)
LOG_FILE=alphaforge-anton-frontend.log          # Log filename
